#include <stdio.h>
/**
* main - that prints the number of arguments passed into it.
* @argc: counts arguments
* @argv: vector for argyement
* Return: Returns (0) Always
*/

int main(argc, char *argv[])
{
	(void) argv;

	printf("%d", argc);
	return (0);
}
